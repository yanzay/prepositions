# Build dependencies for english_prepositions_anki.apkg
#
# Core: official Anki backend (replaces deprecated genanki — produces
# modern v18 .apkg files where deck-options presets actually bind on
# import in Anki Desktop 23.10+).
anki>=24.0

# Tier-2 media generation (all optional — build_anki_package.py works
# without them; the deck simply ships without that media layer).
google-cloud-texttospeech>=2.16.0   # for build_audio.py
eng-to-ipa>=0.0.2                   # for build_ipa.py
# build_images.py uses ONLY stdlib (urllib) — no extra dep needed for the
# Wikimedia Commons + picsum.photos fetch.

# System prerequisite (NOT pip-installable):
#   ffmpeg — used by normalize_audio.py for EBU R128 loudness pass
#   macOS:  brew install ffmpeg
#   Debian: apt install ffmpeg
